# ==============================================================================
# ENTERPRISE AGENTIC RAG - REQUIREMENTS (Render deploy, Python 3.11.9)
# All versions verified against the project's working .venv (CPython 3.11.9).
# Eval-only extras (ragas, sentence-transformers, torch) were removed from this
# file — the evals/ console is local-only tooling. Install them ad hoc with:
#   pip install ragas==0.4.3 sentence-transformers==5.6.0
# ==============================================================================

# --- CORE API & WEB FRAMEWORK ---
fastapi==0.139.0              # High-performance web framework for the API
uvicorn[standard]==0.51.0     # ASGI server to run the FastAPI app
python-dotenv==1.2.2          # For loading configuration from .env files
requests==2.34.2              # HTTP client (UI -> backend, logfire instrumentation)
streamlit==1.59.1             # Frontend dashboard for the chat interface
nest-asyncio==1.6.0           # Nested async loops (evals/streamlit context)
pydantic==2.13.4              # Data validation (request models)

# --- VECTOR DB & RETRIEVAL ---
qdrant-client==1.18.0         # Vector Database for semantic search
# Reranking: Jina Reranker API (hosted) — no local rerank model dependency

# --- LANGCHAIN & AGENTIC ORCHESTRATION ---
langchain==1.3.13             # Anchors the LangChain 1.x family versions
langchain-core==1.4.9         # Shared runtime for all langchain-* integrations
langchain-text-splitters==1.1.2  # RecursiveCharacterTextSplitter (chunking/splitter.py)
langgraph==1.2.9              # State-machine logic for cyclic agent flows
langchain-openai==1.3.5       # OpenAI-compatible layer via Portkey gateway
langchain-groq==1.1.3         # ChatGroq — guardrails gate LLM (guardrails/rails.py)
langchain-google-genai==4.2.7 # Gemini embeddings (services/retrieval/embedding.py)
nemoguardrails==0.23.0        # NVIDIA NeMo Guardrails input/output safety rails
portkey-ai==2.3.2             # Portkey Gateway: routing, fallbacks, caching
langsmith==0.10.2             # Tracing for LangChain/LangGraph (LANGCHAIN_TRACING_V2)

# --- DATA INGESTION & DOCUMENT PARSING ---
unstructured==0.24.1          # DOCX/PPTX partitioning (ingestion/loaders/office.py)
python-pptx==1.0.2            # PowerPoint parser backend for unstructured
python-docx==1.2.0            # Word parser backend for unstructured
pypdf==6.14.2                 # Primary PDF text extraction
pdfplumber==0.11.10           # Fallback PDF parser for blank/scanned pages
beautifulsoup4==4.15.0        # HTML text extraction

# --- OBSERVABILITY & DISTRIBUTED TRACING ---
logfire[fastapi,requests]==4.37.0  # Pydantic Logfire tracing + FastAPI/requests hooks
